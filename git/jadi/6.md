git branch <branch_name>
git checkout <branch_name>

git checkout -b <branch_name>

# in source branch
git merge <targer_branch>

git branch -d <branch_name> # delete branch

git merge --abort

git branch -D <branch_name>
git log --graph
git log --oneline
