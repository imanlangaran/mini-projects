git merge

# in source branch
git rebase <target_name[hash|origin/main|...]>

git rebase --abort
fix conflict
git add <resolved_conflict_file_name>
git rebase --continue

git push -d <origin_name> <branch_name>


3 way merge