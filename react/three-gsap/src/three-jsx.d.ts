/**
 * Ambient JSX typing for R3F under strict mode. R3F v8 ships its own
 * element types; this file exists to surface any gaps explicitly rather
 * than letting the build silently widen. If drei/fiber types are already
 * sufficient, this stays empty (as intended).
 */
import "@react-three/fiber";